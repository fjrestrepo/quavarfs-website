<section class="hero">
  <div class="container hero__grid">
    <div class="hero__content">
      <p class="eyebrow">QUAVAR Facility Services</p>

      <h1>Building Better Environments.</h1>

      <p class="hero__text">
        Facility Services for professional and high-performance environments.
      </p>

      <div class="hero__actions">
        <a class="button button-primary" href="/contact/#reach-quavar">Request a Proposal</a>
        <a class="button button-secondary" href="#services">Explore Services</a>
      </div>
    </div>

    <div class="hero__media">
      <img
        src="/img/hero/quavar-hero-lobby.webp"
        alt="Modern professional lobby environment"
        width="1536"
        height="1024"
        fetchpriority="high"
        decoding="async"
      />
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 88px 0 136px;
    background:
      linear-gradient(90deg, rgba(0, 30, 61, 0.028), rgba(255, 255, 255, 0) 42%),
      var(--color-white);
  }

  .hero__grid {
    display: grid;
    grid-template-columns: minmax(0, 500px) minmax(0, 1fr);
    gap: 96px;
    align-items: center;
  }

  .hero__content {
    max-width: 500px;
  }

  .hero h1 {
    margin-top: 24px;
    max-width: 520px;
    font-size: clamp(52px, 5vw, 72px);
  }

  .hero__text {
    margin-top: 28px;
    max-width: 460px;
    font-size: 20px;
    line-height: 1.55;
  }

  .hero__actions {
    display: flex;
    flex-wrap: wrap;
    gap: 16px;
    margin-top: 40px;
  }

  .hero__media {
    position: relative;
    width: 100%;
    max-width: 620px;
    justify-self: end;
    min-height: 528px;
    overflow: hidden;
    border-radius: 24px;
    background: var(--color-light-gray);
    box-shadow: var(--shadow-soft);
  }

  .hero__media::before {
    content: "";
    position: absolute;
    inset: 0;
    z-index: 1;
    background:
      linear-gradient(180deg, rgba(0, 30, 61, 0) 50%, rgba(0, 30, 61, 0.24)),
      linear-gradient(90deg, rgba(0, 30, 61, 0.1), rgba(0, 30, 61, 0));
    pointer-events: none;
  }

  .hero__media::after {
    content: "";
    position: absolute;
    right: 36px;
    top: 36px;
    z-index: 2;
    width: 88px;
    height: 88px;
    border-top: 1px solid rgba(255, 255, 255, 0.56);
    border-right: 1px solid rgba(255, 255, 255, 0.56);
    pointer-events: none;
  }

  .hero__media img {
    width: 100%;
    height: 100%;
    min-height: 528px;
    object-fit: cover;
  }

  @media (max-width: 1024px) {
    .hero {
      padding: 72px 0 104px;
    }

    .hero__grid {
      grid-template-columns: 1fr;
      gap: 56px;
    }

    .hero__content {
      max-width: 740px;
    }

    .hero h1 {
      max-width: 660px;
    }

    .hero__media {
      justify-self: stretch;
      max-width: none;
      min-height: 460px;
    }

    .hero__media img {
      min-height: 460px;
    }
  }

  @media (max-width: 640px) {
    .hero {
      padding: 48px 0 76px;
    }

    .hero__grid {
      gap: 44px;
    }

    .hero h1 {
      font-size: clamp(40px, 10.6vw, 52px);
      line-height: 1;
    }

    .hero__text {
      font-size: 17px;
      line-height: 1.66;
    }

    .hero__actions {
      flex-direction: column;
      align-items: stretch;
      gap: 12px;
    }

    .hero__media {
      min-height: 340px;
      border-radius: 18px;
    }

    .hero__media::after {
      right: 22px;
      top: 22px;
      width: 64px;
      height: 64px;
    }

    .hero__media img {
      min-height: 340px;
      object-position: center;
    }
  }
</style>